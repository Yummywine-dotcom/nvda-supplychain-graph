# Optional. The API and CLI read the committed snapshot by default.
# Copy this file to .env locally if you want overrides. Never commit secrets.

# Absolute or relative path to an alternate snapshot JSON file.
DATA_SNAPSHOT_PATH=

# Bind address used only when you launch uvicorn yourself.
# UVICORN_HOST=127.0.0.1
# UVICORN_PORT=8000
